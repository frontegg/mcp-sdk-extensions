MCP_SERVER_URL=https://your-app.mcp-gw.stg.frontegg.com
PORT=3000

# Optional: OAuth client ID (omit for Dynamic Client Registration)
# OAUTH_CLIENT_ID=your-client-id

# Optional: stable DPoP key (ES256 private JWK as JSON). If omitted, a fresh key is generated per run.
# DPOP_PRIVATE_JWK={"kty":"EC","crv":"P-256","x":"...","y":"...","d":"..."}

OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o
